# ~/.bashrc.secrets — machine-local secrets, never committed
# Copy this file to ~/.bashrc.secrets and fill in your real values.
# It is sourced automatically by .bashrc.

# Figma MCP server token
export FIGMA_ACCESS_TOKEN="your-figma-token-here"

# Brave Search API key
export BRAVE_API_KEY="your-brave-api-key-here"

# Notion integration token
export NOTION_TOKEN="your-notion-token-here"

# Playwright MCP Bridge extension token
export PLAYWRIGHT_MCP_EXTENSION_TOKEN="your-playwright-token-here"
